import { buttonTheme } from './buttons.js'
import { paginationTheme } from './pagination.js'  // Add this import

export const customTheme = {
  button: buttonTheme,
  pagination: paginationTheme  
}

// Export individual themes if needed
export { buttonTheme, paginationTheme }